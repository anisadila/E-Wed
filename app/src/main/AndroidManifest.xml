<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adila.e_wed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Registrasi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity" />
            <!--android:theme="@style/noBar" />-->
        <activity android:name=".DetailProduk" />
        <activity android:name=".ListProduk" />
        <activity android:name=".OrderForm" />
        <activity android:name=".CheckOrderForm" />
        <activity android:name=".CartOrderForm" />
        <activity android:name=".PaymentConfirmation" />
        <activity android:name=".FAQ"
            android:parentActivityName=".FAQ"/>
        <activity android:name=".TermsConditions"
            android:parentActivityName=".TermsConditions"/>
        <activity android:name=".About"
            android:parentActivityName=".About"/>
    </application>

</manifest>