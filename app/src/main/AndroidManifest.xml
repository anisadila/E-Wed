<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adila.e_wed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Login"
            android:theme="@style/noBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Registrasi"
            android:label="Registrasi">
        </activity>
        <activity android:name=".MainActivity" />
        <!-- android:theme="@style/noBar" /> -->
        <activity android:name=".ListProduk" />
        <activity
            android:name=".DetailProduk"
            android:label="Detail Produk"
            android:parentActivityName=".ListProduk" />
        <activity
            android:name=".OrderForm"
            android:label="Order Form"
            android:parentActivityName=".DetailProduk" />
        <activity
            android:name=".CheckOrderForm"
            android:label="Order Form"
            android:parentActivityName=".OrderForm" />
        <activity
            android:name=".CartOrderForm"
            android:label="Order Form"
            android:parentActivityName=".CartOrderForm" />
        <activity
            android:name=".PaymentConfirmation"
            android:label="Payment Confirmation"
            android:parentActivityName=".CartOrderForm" />
        <activity
            android:name=".FAQ"
            android:label="FAQ"
            android:parentActivityName=".FAQ" />
        <activity
            android:name=".TermsConditions"
            android:label="Terms &amp; Conditions"
            android:parentActivityName=".TermsConditions" />
        <activity
            android:name=".About"
            android:label="About"
            android:parentActivityName=".About" />

    </application>

</manifest>